Import-Module SqlServer

$server = "your-mi-name.public.xxxxx.database.windows.net,3342"
$database = "YourDatabase"

Invoke-Sqlcmd `
    -ServerInstance $server `
    -Database $database `
    -Authentication "Active Directory Integrated" `
    -Query "SELECT SUSER_SNAME();"
